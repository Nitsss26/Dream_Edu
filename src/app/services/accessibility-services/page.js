import SemService from "./page1"

export const metadata = {
    title: 'Dream Education / Accessibility Service',
    description: 'Expand online accessibility for broader audience',
}

const page = () => {
    return (
        <SemService />
    )
}

export default page