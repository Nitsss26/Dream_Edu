import React from 'react'
import BookWritingService from './page1'

export const metadata = {
    title: 'Dream Education / Books Writing',
    description: 'Professional book writing services.',
}

const page = () => {
    return (
        <BookWritingService />
    )
}

export default page